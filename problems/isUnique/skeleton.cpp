#include<bits/stdc++.h>
using namespace std;

bool isUnique(string s) {
    // Your code here
}

int main() {
    string s;
    cin >> s;
    cout << (isUnique(s) ? "True" : "False") << endl;
    return 0;
}
